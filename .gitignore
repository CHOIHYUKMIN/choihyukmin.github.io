# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
logs/
*.log

# Temporary files
*.tmp
*.temp

# Build outputs (if any)
dist/
build/

# Local environment
.env
.env.local

# Capacitor / Android
android/app/build/
android/app/release/
android/.gradle/
android/build/
*.keystore
*.jks

# www/ is tracked in git so Capacitor can sync locally without running copy-www.
# GitHub Actions regenerates www/ fresh on every build (see .github/workflows/android-build.yml).
# To sync locally: npm run copy-www && npx cap sync
www/.DS_Store
